apply plugin: 'android'

dependencies {
    compile fileTree(dir: 'libs', include: '*.jar')
    compile project(':Google Drive:Libraries:google_play_services:libproject:google-play-services_lib')
    compile project(':Google Drive:Libraries:slidingMenu')
    compile project(':Google Drive:Libraries:ViewPagerIndicator')
    compile project(':Google Drive:Libraries:actionbarsherlock')
}

android {
    compileSdkVersion 17
    buildToolsVersion "20.0.0"

    sourceSets {
        main {
            manifest.srcFile 'AndroidManifest.xml'
            java.srcDirs = ['libs/nineoldandroids','src']
            resources.srcDirs = ['libs/nineoldandroids','src']
            aidl.srcDirs = ['libs/nineoldandroids','src']
            renderscript.srcDirs = ['libs/nineoldandroids','src']
            res.srcDirs = ['res']
            assets.srcDirs = ['assets']
        }

        // Move the tests to tests/java, tests/res, etc...
        instrumentTest.setRoot('tests')

        // Move the build types to build-types/<type>
        // For instance, build-types/debug/java, build-types/debug/AndroidManifest.xml, ...
        // This moves them out of them default location under src/<type>/... which would
        // conflict with src/ being used by the main source set.
        // Adding new build types or product flavors should be accompanied
        // by a similar customization.
        debug.setRoot('build-types/debug')
        release.setRoot('build-types/release')
    }
}
